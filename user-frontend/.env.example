# Public API base URL. Used at build time AND runtime in the browser.
# Local dev (XAMPP): http://localhost/bookingproject/api
# Production: https://your-domain.com/api  (the PHP host where api/ is uploaded)
NEXT_PUBLIC_API_URL=http://localhost/bookingproject/api
